container_commands:
  01_migrations:
    command: "php artisan migrate --force"
    leader_only: true
  02_optimize:
    command: "php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan storage:link || true"
